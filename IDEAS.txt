Reimplement the new shadow sampler thing and try to optimize the shadow softening algorithm. (DONE)
Consider fixing the point light with the new idea. (DONE)
Work on the new HUD system.
Implement a neat and efficient transparency system. Apply to sprites and HUD.
It may be a good idea to just ditch the subtype system and just make them their own shaders again. It is too clunky as is.
Combine the two asset projects into 3DRAsset program.
Implement sprites.
Clean up and standardize the commands, add a console HUD element for command response.
Bug check everything, specifically the shaders system.
Work on any light changes and updates. For instance, consider edge cases with soft shadow sampling radius, CSM changing soft shadow sample radius, more efficient methods of sampling, etc.
Consider implementing baked lighting for certain situations. Since it will mostly be realtime with realtime effects, this will be limited. But, for example, an always-on light should easily be able to bake lighting cast from static objects onto static objects.
Begin work on a simple level loading system, or rather, a functionality which loads levels in conjunction with displaying the load progress with the use of a HUD element.
Work on adding an audio system for the engine.
Figure out an easy way to do reflections.
Figure out an easy way to do water effects.
Physics engine: implement physics engine for physical interraction as well as possibly a method for rendering (view frustum, see if render collision boxes collide, if they do, render)